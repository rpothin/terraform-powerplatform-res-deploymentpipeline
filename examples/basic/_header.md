# Basic Example

This example creates a minimal development-to-test deployment pipeline without Entra ID sharing.
To enable sharing, provide the `security_group_id` argument pointing to an Entra ID security group.
